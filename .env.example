# Server-side only. Set this in Vercel Project Settings -> Environment Variables.
# Do not expose this with a VITE_ prefix.
GEMINI_API_KEY=

# Optional. Defaults to gemini-2.5-flash in api/concierge.ts.
GEMINI_MODEL=gemini-2.5-flash
